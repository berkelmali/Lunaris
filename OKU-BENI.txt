================================================================================
  🌕 LUNARIS — KAPSAMLI TEKNİK DOKÜMANTASYON & FONKSİYON REFERANS KILAVUZU 🌕
================================================================================

Bu doküman, LUNARIS projesindeki tüm mimariyi, makine öğrenmesi algoritmalarını,
Firebase servislerini, matematiksel modelleri ve JavaScript fonksiyonlarını
en ince ayrıntısına kadar açıklar.

────────────────────────────────────────────────────────────────────────────────
📁 1. PROJE DOSYA MİMARİSİ VE ROLLERİ
────────────────────────────────────────────────────────────────────────────────

Klasörde yer alan 7 dosya birbiriyle entegre olarak çalışır:

1. `index.html` (Ana Portal)
   - Tarot 3'lü Açılım (Geçmiş-Şimdi-Gelecek)
   - 12 Burç İnteraktif Zodyak Çarkı & Günlük Yorum Paneli
   - 🧠 ML Tabanlı Derin Astroloji & Doğum Haritası (Natal Harita + Radar Grafik)
   - Günün Yıldız Haritası & Gerçek Astronomi Fotoğrafları
   - İki Burç Arası Sinerji ve Uyum Hesaplayıcı
   - Kolektif Yıldız Tarlası & Dilek Feneri (Canvas tabanlı)
   - Gerçek Zamanlı Topluluk Duvarı

2. `lunaris-ml.js` (Makine Öğrenmesi & Astroloji Motoru)
   - Dış kütüphanesiz, saf JavaScript deterministik sinir ağı simülasyonu
   - 8 boyutlu Zodyak Feature Vector uzayı
   - Keldani Gezegensel Saat matrisi
   - J2000 referanslı Gezegen Transit ve Retrograd motoru
   - Güneş, Ay ve Yükselen Burç hesaplayan Natal Harita algoritması
   - Dot-Product ve Sigmoid aktivasyonlu çoklu kategori skorlayıcı
   - 12 Aylık Gelecek Enerji Zaman Çizelgesi simülatörü
   - Çok dilli (TR / EN / RU) ağırlıklı metin havuzu

3. `araclar.html` (Ezoterik & Kozmik Araçlar)
   - Kutsal Numeroloji (Yaşam Yolu Sayısı & Usta Sayılar 11, 22, 33)
   - Doğu Astrolojisi (12 Hayvan Totemi & 5 Element Mührü)
   - Batı Zodyak Dekanları & 3D Enerji Dengesi
   - 2026 Astronomik Takvimi (Merkür Retroları & Güneş/Ay Tutulmaları)
   - Dinamik Aylık Ay Evresi Takvimi

4. `blog.html` (Mistik Notlar)
   - Kozmik makaleler, Ay ritüelleri ve kristal rehberleri
   - Tam ekran okuma modalleri ve dinamik etiket filtreleme

5. `style.css` (Kozmik Tasarım Sistemi & Animasyonlar)
   - Glassmorphic (buzlu cam) koyu tema paleti
   - SVG Radar Grafik, Dairesel Enerji Barları ve 3D Kart Tilt Efektleri
   - Altın ve ametist ışıltılı parçacık animasyonları

6. `main.js` (Ortak Arayüz, Dil & Çekirdek Kütüphane)
   - TR / EN / RU çoklu dil motoru ve localStorage kalıcılığı
   - Mistik Preloader açılış yöneticisi
   - 40+ vektörel SVG ikon ve takımyıldız çizim motoru
   - Toast bildirim sistemi ve genel modal yöneticileri

7. `firebase-config.js` (Merkezi Firebase & Offline Demo Servisi)
   - Firebase Auth (E-posta/Şifre, Google ile Giriş, Şifre Sıfırlama)
   - Cloud Firestore (Gerçek zamanlı duvar akışı, tarot/burç/dilek geçmişi)
   - Firebase olmadan da tam çalışan Akıllı Demo/Misafir Modu


================================================================================
🧠 2. MAKİNE ÖĞRENMESİ & ALGORİTMA MOTORU (`lunaris-ml.js` v2.0)
================================================================================

Tüm fonksiyonlar `window.LunarisML` küresel nesnesi üzerinden erişilebilir durumdadır.

--------------------------------------------------------------------------------
A) Matematiksel Çekirdek Fonksiyonlar (Dahili Yardımcılar):
--------------------------------------------------------------------------------
• `sigmoid(x)`
  - Formül: 1 / (1 + e^(-x))
  - Görevi: Ham ağırlıklı skorları 0.0 ile 1.0 arasında normalize eder.

• `relu(x)` & `leakyRelu(x)`
  - Formül: max(0, x) / (x > 0 ? x : 0.01 * x)
  - Görevi: MLP gizli katmanlarında non-lineer aktivasyon sağlar.

• `dot(vectorA, vectorB)`
  - Formül: ∑ (A[i] * B[i])
  - Görevi: Vektörlerin skaler çarpımını alır.

• `matVecMul(matrix, vector)`
  - Formül: M × V matris-vektör çarpımı
  - Görevi: MLP katmanları arası tensör dönüşümü.

• `normalize(vector)` & `cosineSimilarity(a, b)`
  - Formül: Öklid normu ve kosinüs benzerliği hesabı.

• `hashStr(seedString)`
  - Deterministik DJB2 tabanlı hash algoritması. Kararlı sonuç üretir.

• `julianDay(date)`
  - Verilen miladi tarihi Astronomik Jülyen Gün Sayısına çevirir.

--------------------------------------------------------------------------------
B) Vektör Uzayı & 16 Katmanlı Mimari:
--------------------------------------------------------------------------------
1. 8 Boyutlu Burç Vektörü (`SIGN_VECTORS`):
   Boyutlar: [0: Enerji, 1: İstikrar, 2: Macera, 3: Pratiklik, 4: Sosyallik, 5: Sezgi, 6: Liderlik, 7: Duyarlılık]

2. Element & Modalite Uyum Matrisi (`ELEMENT_HARMONY`, `MODALITY_HARMONY`):
   Ateş, Toprak, Hava ve Su elementleri; Kardinal, Sabit ve Değişken modaliteler arası sinerji.

3. 3-Katmanlı MLP (Multi-Layer Perceptron) Modeli:
   - Input Layer (16D): 8D Burç Vektörü + 8D Temporal Vektör
   - Hidden Layer 1 (12D, Leaky ReLU + Natal Attention)
   - Hidden Layer 2 (8D, Leaky ReLU)
   - Output Layer (6D, Sigmoid): Aşk, Şans, Kariyer, Sağlık, Para, Günlük

4. Aspect Engine (Gezegen Açıları):
   Kavuşum (0°), Sekstil (60°), Kare (90°), Üçgen (120°), Karşıt (180°) açı tespiti ve orbsal etki hesabı.

5. Gelişmiş Retrograd & Gölge Periyodu Sistemi:
   5 gezegen (Merkür, Venüs, Mars, Jüpiter, Satürn) için tarih bazlı retro ve gölge dönemleri.

6. 5-Seviyeli Metin Havuzu:
   Peak (≥0.82), High (≥0.65), Mid (≥0.42), Low (≥0.25), Critical (<0.25) olmak üzere 80+ zengin metin.

--------------------------------------------------------------------------------
C) Dışa Açık API Fonksiyonları (`LunarisML`):
--------------------------------------------------------------------------------

1. `LunarisML.generateDeepReading(signKey, lang, birthDate, birthHour)`
   - Günlük detaylı ML yorumu, radar verisi, retrograd/aspect uyarıları ve şanslı öğeleri üretir.

2. `LunarisML.generateWeeklyReading(signKey, lang, birthDate, birthHour)`
   - 7 günlük detaylı tahmin, gün bazında puanlama, hafta teması, en iyi ve en zorlu gün analizi.

3. `LunarisML.generateMonthlyReading(signKey, lang, birthDate, birthHour)`
   - 30 günlük trend analizi, 4 haftalık kırılım, ay teması ve retrograd takibi.

4. `LunarisML.calculateDeepCompatibility(sign1, sign2, lang)`
   - İki burç arasında çok boyutlu uyum analizi: Vektörel Benzerlik, Element, Modalite, Gezegen Sinerjisi;
   - Kategori skorları: Aşk, İletişim, Güven, Tutku, Uzun Vade; Doğal Güçler & Sürtüşme Noktaları.

5. `LunarisML.calcNatalScores(birthDate, birthHour)`
   - Güneş, Ay ve Yükselen Burç hesaplayıp 8 boyutlu natal profil üretir.

6. `LunarisML.generateEnergyTimeline(signKey, natalVector)`
   - Gelecek 12 ayın Enerji, Aşk, Kariyer, Şans ve Para projeksiyonunu döner.

7. `LunarisML.getCategoryScores(signKey, date, natalVector)`
   - 6 kategoride hibrit (MLP + Legacy + Transit + Aspect) matematiksel skorları döner.

8. `LunarisML.getRetrogrades(date)` & `LunarisML.calcAspects(date)`
   - Aktif gezegen retrogradları, gölge dönemleri ve anlık gezegen açıları.


================================================================================
🌐 3. MERKEZİ FIREBASE & KİMLİK SERVİSLERİ (`firebase-config.js`)
================================================================================

Tüm servisler `window.LunarisAuth` ve `window.LunarisWall` nesneleri üzerinden sunulur.

--------------------------------------------------------------------------------
A) `window.LunarisAuth` (Kullanıcı & Hesap Yönetimi):
--------------------------------------------------------------------------------

• `LunarisAuth.isLoggedIn()`
  - Kullanıcının oturum açmış olup olmadığını döner (boolean).

• `LunarisAuth.getUser()`
  - Aktif kullanıcı nesnesini döner:
    { uid, email, displayName, username, photoURL, isDemo, preferredLang }

• `LunarisAuth.login(email, password)`
  - Firebase Authentication ile e-posta/şifre girişi yapar.
  - Hata yakalama: `auth/wrong-password`, `auth/user-not-found`, `auth/too-many-requests`.

• `LunarisAuth.signup(email, password, username)`
  - Yeni kullanıcı oluşturur, Firestore `users/{uid}` koleksiyonuna kullanıcı adı ve profilini yazar.

• `LunarisAuth.loginWithGoogle()`
  - `GoogleAuthProvider` ve `signInWithPopup` ile tek tıkla Google girişi sağlar.

• `LunarisAuth.loginDemo()`
  - Firebase yapılandırılmamış olsa dahi rastgele kullanıcı kimliği ile yerel demo oturumu açar.
  - Verileri tarayıcının LocalStorage hafızasında tutar.

• `LunarisAuth.sendPasswordReset(email)`
  - Firebase üzerinden şifre sıfırlama e-postası yollar.

• `LunarisAuth.logout()`
  - Oturumu kapatır, yerel hafızayı temizler ve arayüzü misafir moduna geçirir.

• `LunarisAuth.updatePhoto(photoURL)`
  - Kullanıcının profil fotoğrafı URL'sini Firebase Auth ve Firestore `users/{uid}` üzerinde günceller.

• `LunarisAuth.saveReading(cardKeys, comboText, lang)`
  - Yapılan tarot açılımını Firestore `readings` koleksiyonuna kaydeder.
  - Demo modunda ise `lunaris_local_readings` LocalStorage dizisine ekler.

• `LunarisAuth.getHistory()`
  - Kullanıcının geçmiş tarot fallarını tarihe göre azalan sırada getirir.

• `LunarisAuth.saveHoroscope(horoData)`
  - Seçilen burç yorumunu (aşk, şans, para metinleri ve şanslı sayılarıyla birlikte) kaydeder.

• `LunarisAuth.getHoroscopeHistory()`
  - Kullanıcının kaydettiği tüm burç yorumlarını listeler.

• `LunarisAuth.saveWish(wishText, category)`
  - Dilek Feneri'ne bırakılan niyeti Firestore `wishes` koleksiyonuna veya yerel hafızaya kaydeder.

• `LunarisAuth.getMyWishes()`
  - Kullanıcının kendi fırlattığı dilekleri ve toplanan ışık/enerji sayılarını getirir.

--------------------------------------------------------------------------------
B) `window.LunarisWall` (Topluluk Duvarı & Canlı Akış):
--------------------------------------------------------------------------------

• `LunarisWall.isConfigured()`
  - Firestore bağlantısının hazır olup olmadığını kontrol eder.

• `LunarisWall.loadMessages()`
  - Duvardaki son 50 mesajı Firestore'dan tarihe göre sıralayarak çeker.

• `LunarisWall.subscribeMessages(callback)`
  - Firestore `onSnapshot` gerçek zamanlı dinleyicisini kurar. Duvarda biri yeni mesaj
    paylaştığında veya beğendiğinde sayfayı yenilemeden arayüzü anlık günceller.

• `LunarisWall.postMessage(text, mood)`
  - Yeni duvar mesajı oluşturur (Yazar adı, profil resmi, ruh hali ikonu ve zaman damgasıyla).

• `LunarisWall.likeMessage(id)`
  - Firestore `increment(1)` atomik işlemi ile gönderinin beğeni sayısını artırır.

• `LunarisWall.deleteMessage(id)`
  - Kullanıcının sadece kendi paylaştığı duvar mesajlarını silmesini sağlar.


================================================================================
🛠️ 4. ORTAK ARAYÜZ, DİL & YARDIMCI FONKSİYONLAR (`main.js`)
================================================================================

• `setLang(lang)`
  - Parametre: `'tr'` | `'en'` | `'ru'`
  - Dil değişiminde tüm sayfadaki metinleri, zodyak çarkını, açılmış olan tarot kartlarını,
    ML derin analiz sonuçlarını ve topluluk duvarını anında seçilen dile çevirir.

• `applyLangToDOM(lang)`
  - `data-i18n` (metin içeriği) ve `data-i18n-ph` (placeholder) etiketlerini `UI[lang]` sözlüğü ile eşleştirir.

• `getInitialLang()`
  - LocalStorage veya tarayıcı varsayılanından kullanıcının dilini tespit eder.

• `showToast(message)`
  - Ekranın alt kısmında beliren, altın parıltılı, 3 saniye sonra kaybolan toast uyarısı gösterir.

• `icon(name, size, className)`
  - 40'tan fazla vektörel SVG simgesini (Tarot kartları, gezegenler, elementler, çakralar) üretir.

• `renderConstellationSVG(signKey, className)`
  - 12 Zodyak burcunun mitolojik takımyıldız yıldız-çizgi haritalarını SVG olarak çizer.

• `moonIconSVG(phaseIndex, idPrefix)`
  - Ay'ın 8 farklı evresini aydınlanma açısına göre gölgeli SVG olarak üretir.

• `createStars(count)`
  - Canvas ve DOM üzerinde rastgele boyut ve parlaklıkta yıldız tarlası (Starfield) inşa eder.

• `spawnShootingStar()`
  - Belirli aralıklarla gökyüzünden çapraz açıyla kayan parlak kuyruklu yıldız animasyonu tetikler.

• `spawnSparkleBurst(x, y)`
  - Tıklanan koordinatta altın ve mor mikro-parçacık patlaması animasyonu oluşturur.

• `initSparkleTrail()`
  - Fare imlecinin arkasında süzülen sihirli toz izi efektini başlatır.

• `initScrollReveal(selector)`
  - IntersectionObserver API ile sayfa aşağı kaydırıldıkça kartların yumuşakça parlamasını sağlar.

• `initNavSpy(sectionIds)`
  - Kullanıcının ekranda gördüğü bölüme göre üst menüdeki linki otomatik aktif (highlight) yapar.

• `openAuthModal(tab)` / `closeAuthModal()`
  - 'login', 'signup' veya 'forgot' sekmeleriyle giriş modalını açar/kapatır.

• `openAccountModal()` / `closeAccountModal()`
  - Profil kartını, avatar seçicisini ve Tarot/Burç/Dilek geçmiş sekmelerini yönetir.

• `setProfilePhoto(url)` / `handleCustomPhotoUpload(event)`
  - Hazır kozmik avatarları seçmeyi veya kullanıcının cihazından özel fotoğraf yüklemesini sağlar.


================================================================================
🌟 5. ANA SAYFA FONKSİYONLARI (`index.html`)
================================================================================

[A. ML Derin Astroloji & Doğum Haritası]:
• `runDeepReading()`
  - Doğum tarihi ve saatini alıp `LunarisML.generateDeepReading()` fonksiyonunu çalıştırır.
  - Radar grafiği, enerji halkası, derin yorum kartları ve 12 aylık zaman çizelgesini doldurur.

• `drawRadarChart(profile, lang)`
  - 8 boyutlu kişilik profilini matematiksel trigonometrik açılarla (2π/8) hesaplar,
    SVG poligon ve eksen çizgileri olarak çizer.

• `renderEnergyTimelineUI(timeline, lang)`
  - Gelecek 12 ayın Enerji, Aşk, Kariyer ve Şans çubuk grafiğini ve lejantını oluşturur.

• `renderDeepReadingUI(data)`
  - Tüm ML analiz sonuçlarını, retrograd uyarılarını ve içgörü panellerini arayüze basar.

• `quickFillDeepSign(signKey)`
  - Burç çarkından tıklanan burcun yaklaşık doğum tarihini forma otomatik yazar ve
    sayfayı pürüzsüz kaydırarak (smooth scroll) analizi anında başlatır.

[B. Burç Yorumları & Zodyak Çarkı]:
• `buildWheel()`
  - 36 derecelik açılarla 12 burç butonunu ve dönen gezegen yörüngelerini inşa eder.

• `selectSign(key)`
  - Seçilen burcun aktif durumunu günceller ve ML destekli burç detay panelini açar.

• `renderHoroPanel()`
  - Aşk, Şans, Günlük Hayat ve Para yorumlarını, şanslı rengi, sayıyı ve
    ML Kozmik Titreşim skorunu (`LunarisML.getCategoryScores`) panele basar.

• `saveActiveHoroscope()`
  - Görüntülenen burç yorumunu kullanıcının profil geçmişine kaydeder.

[C. Tarot Açılımı]:
• `drawSpread()`
  - 22 kartlık desteyi rastgele karıştırır ve 3 kart seçer.

• `renderSpread(animate)`
  - Kartları 3D çevirme (flip) animasyonuyla açar, ses/parıltı efektleri verir ve
    `renderComboReading()` ile birleşik kehanet metnini üretir.

[D. Burç Uyumu & Yıldız Haritası]:
• `buildCompatSelectors()` → 1. ve 2. burç açılır menülerini doldurur.
• `checkCompatibility()` → İki burcun element ve modalite sinerjisini hesaplayıp yüzde halkasını çizer.
• `updateMoonDisplay()` & `updateStarMap()` → Günün Ay evresini ve gökyüzü fotoğrafını günceller.

[E. Dilek Feneri & Topluluk]:
• `submitWish()` → HTML5 Canvas üzerine yeni dilek yıldızı fırlatır ve veritabanına kaydeder.
• `submitWallPost()` → Topluluk duvarına mesaj paylaşır (10 saniyelik spam korumasıyla).
• `likeWallPost(id)` & `deleteWallPost(id)` → Duvar mesajı etkileşimlerini yönetir.


================================================================================
🔮 6. KOZMİK ARAÇLAR SAYFASI FONKSİYONLARI (`araclar.html`)
================================================================================

• `calculateBirthMatrix()`
  - Doğum tarihini alarak Numeroloji, Çin Burcu ve Güneş İmzası analizlerini tek tıkla çalıştırır.

• `lifePathNumber(day, month, year)`
  - Doğum tarihinin tüm basamaklarını toplayıp tek haneye veya 11, 22, 33 usta sayılarına indirger.

• `renderNumerologyResult(num, day, month, year)`
  - Yaşam yolu sayısını, arketip unvanını, Solfeggio titreşim frekansını (Hz),
    çakrasını ve yönetici gezegenini gösterir.

• `getChineseZodiac(year)`
  - Ay takvimi döngüsüne göre 12 Hayvan Totemini ve 5 Element (Ahşap, Ateş, Toprak, Metal, Su) mührünü bulur.

• `renderChineseResult(animalIdx, elementKey)`
  - Çin burcu totem görselini, Yin/Yang polaritesini ve element simyasını çizer.

• `renderWesternResult(day, month)`
  - Güneş burcunun dekanlarını, zihinsel odak, ruhsal sezgi ve manyetik çekim oranlarını gösterir.

• `renderCosmicCalendar()`
  - 2026 yılındaki tüm Merkür Retrosu dönemlerini ve Güneş/Ay tutulmalarını listeler.

• `getMercuryStatus(date)`
  - Şu anda Merkür retrosu olup olmadığını, bitimine veya bir sonraki retroya kaç gün kaldığını hesaplar.

• `renderMoonCalendar()` & `navigateMoonMonth(dir)`
  - Ayın her günü için o günkü Ay evresi simgesini içeren aylık interaktif takvimi çizer.


================================================================================
📖 7. BLOG SAYFASI FONKSİYONLARI (`blog.html`)
================================================================================

• `renderBlogList()`
  - Ay evreleri, kristal enerjisi, gezegen hareketleri ve tarot sırları konulu
    tüm makaleleri kartlar halinde listeler.

• `openBlogModal(articleKey)` / `closeBlogModal()`
  - Seçilen makalenin tam metnini, okuma süresini ve kozmik tavsiyelerini cam efektli okuma penceresinde açar.


================================================================================
🚀 8. ÇALIŞTIRMA & TEST KILAVUZU
================================================================================

1. Yerel Çalıştırma:
   - `index.html` dosyasına çift tıklayarak tarayıcınızda (Chrome, Edge, Safari, Firefox)
     doğrudan açabilirsiniz. Herhangi bir sunucu veya kurulum gerektirmez.

2. Firebase Yapılandırması (Opsiyonel):
   - Firebase bağlamak için `firebase-config.js` dosyasındaki `firebaseConfig`
     nesnesine kendi projenizin API anahtarlarını yazmanız yeterlidir.
   - Firebase bağlanmasa dahi site Akıllı Demo Modu ile tüm özellikleriyle kusursuz çalışır.

3. Makine Öğrenmesi Doğrulama:
   - Node.js kurulu ortamda `lunaris-ml.js` motoru doğrudan test edilebilir.
   - Tüm matematiksel hesaplamalar deterministiktir ve sıfır dış bağımlılıkla çalışır.

4. 📱 Mobil Uygulama (Android & iOS) Çalıştırma:
   - `npm run sync`         → Web dosyalarını Android/iOS projelerine aktarır.
   - `npm run open:android` → Android Studio'da açar (.apk / .aab derlemek için).
   - `npm run open:ios`     → Xcode'da açar (iOS simülatör/cihaz için).

